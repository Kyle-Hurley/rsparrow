% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsparrow_model.R
\name{rsparrow_estimate}
\alias{rsparrow_estimate}
\title{Estimate a SPARROW Model}
\usage{
rsparrow_estimate(
  data,
  run_id = "run1",
  output_dir = NULL,
  if_estimate = TRUE,
  if_predict = TRUE,
  if_validate = FALSE,
  hessian = TRUE,
  mean_adjust = TRUE,
  weights = "default",
  s_offset = 1e+14,
  load_units = "kg/year",
  yield_units = "kg/ha/year",
  yield_factor = 0.01,
  conc_units = "mg/L",
  conc_factor = 1
)
}
\arguments{
\item{data}{An object of class \code{"rsparrow_data"} returned by
\code{\link{rsparrow_prepare}}.}

\item{run_id}{Character. Name for this model run (default: \code{"run1"}).}

\item{output_dir}{Character or \code{NULL}. Directory for file output.
\code{NULL} (default) runs entirely in memory with no file I/O.}

\item{if_estimate}{Logical. \code{TRUE} (default) runs NLLS optimisation.
\code{FALSE} uses initial parameter values without fitting.}

\item{if_predict}{Logical. \code{TRUE} (default) computes reach-level
predictions after estimation.}

\item{if_validate}{Logical. \code{FALSE} (default). \code{TRUE} splits
monitoring sites into calibration and validation sets.}

\item{hessian}{Logical. \code{TRUE} (default) computes the Hessian
(required for \code{\link{vcov.rsparrow}}).}

\item{mean_adjust}{Logical. \code{TRUE} (default) applies mean-adjustment
to delivery variables.}

\item{weights}{Character. NLLS weighting scheme. \code{"default"} (scalar
1), \code{"lnload"}, or \code{"user"}.}

\item{s_offset}{Numeric. Offset for objective function scaling
(default: \code{1e14}).}

\item{load_units}{Character. Units label for loads (default:
\code{"kg/year"}).}

\item{yield_units}{Character. Units label for yields (default:
\code{"kg/ha/year"}).}

\item{yield_factor}{Numeric. Conversion factor for yields (default:
\code{0.01}).}

\item{conc_units}{Character. Units label for concentrations (default:
\code{"mg/L"}).}

\item{conc_factor}{Numeric. Conversion factor for concentrations (default:
\code{1.0}).}
}
\value{
An S3 object of class \code{"rsparrow"}. See
\code{\link{rsparrow_model}} for the full structure description.
}
\description{
Fits a SPARROW model using nonlinear least squares given a prepared data
object from \code{\link{rsparrow_prepare}}. This is the second step of the
composable workflow.
}
\examples{
\donttest{
sparrow_data <- rsparrow_prepare(
  sparrow_example$reaches,
  sparrow_example$parameters,
  sparrow_example$design_matrix
)
model <- rsparrow_estimate(sparrow_data)
coef(model)
}
}
\seealso{
\code{\link{rsparrow_prepare}}, \code{\link{rsparrow_model}},
\code{\link{predict.rsparrow}}, \code{\link{coef.rsparrow}}
}
